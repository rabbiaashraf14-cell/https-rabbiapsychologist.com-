---
import ServiceCardComponent from './services/ServiceCard.astro';
const { title, description, href } = Astro.props;
const service = { title, description, slug: href?.split('/').pop() || '' };
---
<ServiceCardComponent service={service} />
